# ---- Lambda startup optimizations ----
spring.main.banner-mode=off
spring.main.web-application-type=none
spring.main.lazy-initialization=true
spring.jmx.enabled=false

# Keep logging lean (reduce cold start I/O)
logging.level.root=INFO

# ---- REST target config (prefer env vars on Lambda) ----
# target.url=
# target.api-key=

# ---- Auth (Secrets Manager + STS token endpoint) ----
# secrets.client-credentials-name=
# sts.token-url=
# sts.scope=
